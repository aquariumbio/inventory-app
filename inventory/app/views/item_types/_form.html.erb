<%= form_with(model: item_type) do |form| %>
  <% if item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_type.errors.count, "error") %> prohibited this item_type from being saved:</h2>

      <ul>
        <% item_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :format_type_id %>
    <%= form.number_field :format_type_id %>
  </div>

  <div class="field">
    <%= form.label :format_type_class %>
    <%= form.text_field :format_type_class %>
  </div>

  <div class="field">
    <%= form.label :container_type_id %>
    <%= form.text_field :container_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
