<%= form_with(model: physical_state_type) do |form| %>
  <% if physical_state_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(physical_state_type.errors.count, "error") %> prohibited this physical_state_type from being saved:</h2>

      <ul>
        <% physical_state_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
