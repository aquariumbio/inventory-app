<%= form_with(model: physical_state) do |form| %>
  <% if physical_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(physical_state.errors.count, "error") %> prohibited this physical_state from being saved:</h2>

      <ul>
        <% physical_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
