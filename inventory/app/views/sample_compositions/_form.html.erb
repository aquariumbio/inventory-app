<%= form_with(model: sample_composition) do |form| %>
  <% if sample_composition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample_composition.errors.count, "error") %> prohibited this sample_composition from being saved:</h2>

      <ul>
        <% sample_composition.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_area :quantity %>
  </div>

  <div class="field">
    <%= form.label :composite_sample_id %>
    <%= form.number_field :composite_sample_id %>
  </div>

  <div class="field">
    <%= form.label :component_sample_id %>
    <%= form.number_field :component_sample_id %>
  </div>

  <div class="field">
    <%= form.label :sample_composition_type_id %>
    <%= form.text_field :sample_composition_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
