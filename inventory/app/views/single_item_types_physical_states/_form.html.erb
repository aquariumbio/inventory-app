<%= form_with(model: single_item_types_physical_state) do |form| %>
  <% if single_item_types_physical_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(single_item_types_physical_state.errors.count, "error") %> prohibited this single_item_types_physical_state from being saved:</h2>

      <ul>
        <% single_item_types_physical_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :single_item_type_id %>
    <%= form.text_field :single_item_type_id %>
  </div>

  <div class="field">
    <%= form.label :physical_state_id %>
    <%= form.collection_select(:physical_state_id, PhysicalState.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
